package com.PhotoFilters.Scaling;

import java.awt.*;

public class Bilinear implements Scaler {
    @Override
    public Color[][] scale(Color[][] PixelArray, float factor) {
        return new Color[0][];
    }
}
